const Header = () => {
    return (
        <header class="header">
            <a href="#" class="logo">Doctor</a>
            <nav class="nav-items">
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
            </nav>
        </header>
    );
}

export default Header;